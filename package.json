{
  "name": "calendrier-electron",
  "version": "1.0.0",
  "main": "./dist/back/main.js",
  "scripts": {
    "dev": "npm run build && npm run start",
    "build": "npm run build-back && npm run build-front",
    "build-back": "tsc --project tsconfig.back.json",
    "build-front": "tsc --project tsconfig.front.json",
    "start": "electron ."
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "electron": "^31.3.1",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
